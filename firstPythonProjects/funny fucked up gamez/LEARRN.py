class math():
    def __init__(self, a, b, x, y):
        self.a = int(a)
        self.b = int(b)
        self.x = int(x)
        self.y = int(y)
    def spread(self, a, b, x, y):
        """
        :param a:
        :param b:
        :param x:
        :param y:
        """
        (x + y)*(a + b) = x*a + x*b + y*a +x*b
